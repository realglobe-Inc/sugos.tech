/**
 * Test case for index.
 * Runs with mocha.
 */
'use strict'

const assert = require('assert')
const co = require('co')

describe('index', () => {
  before(() => co(function * () {
  }))

  after(() => co(function * () {
  }))

  it('index', () => co(function * () {
  }))
})

/* global describe, before, after, it */
